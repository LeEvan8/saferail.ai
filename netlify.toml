# Netlify config: serve main.html at site root
# No build step required — this rewrites requests for / to /main.html

[build]
  publish = "."

[[redirects]]
  from = "/"
  to = "/main.html"
  status = 200
  force = true
